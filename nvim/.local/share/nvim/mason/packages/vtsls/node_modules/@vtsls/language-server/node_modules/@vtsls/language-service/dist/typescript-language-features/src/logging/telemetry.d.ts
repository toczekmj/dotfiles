import { IExperimentationTelemetryReporter } from '../experimentTelemetryReporter';
export interface TelemetryProperties {
    readonly [prop: string]: string | number | boolean | undefined;
}
export interface TelemetryReporter {
    logTelemetry(eventName: string, properties?: TelemetryProperties): void;
    logTraceEvent(tracePoint: string, correlationId: string, command?: string): void;
}
export declare class VSCodeTelemetryReporter implements TelemetryReporter {
    private readonly reporter;
    private readonly clientVersionDelegate;
    constructor(reporter: IExperimentationTelemetryReporter | undefined, clientVersionDelegate: () => string);
    logTelemetry(eventName: string, properties?: {
        [prop: string]: string;
    }): void;
    logTraceEvent(point: string, traceId: string, data?: string): void;
}
//# sourceMappingURL=telemetry.d.ts.map